
export const AcademicFaculty = [
  "Faculty of Programming", "Faculty of Web","Faculty of Mathmatics", "Faculty of Buisness"
];

export const academicFacultyOptions = AcademicFaculty.map((item) => ({
  value: item,
  label: item,
}));